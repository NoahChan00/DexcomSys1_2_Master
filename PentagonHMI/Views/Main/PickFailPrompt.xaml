<Window x:Class="PentagonHMI.Views.Main.PickFailPrompt" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        Title="PickFailPrompt" Height="800" Width="600" ResizeMode="CanMinimize" WindowStyle="None">
    <Border BorderBrush="Black" BorderThickness="0.5" Margin="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Label>Please Select Below Action:</Label>
            <Border Grid.Row="1" CornerRadius="5" Background="LightSkyBlue" Margin="10 5 10 10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="*" /> </Grid.RowDefinitions>
                    <Label x:Name="TitleLabel" FontSize="30" HorizontalAlignment="Center" />
                    <Border Grid.Row="1" CornerRadius="5" Background="White" Margin="20 0 20 20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="auto" />
                                <RowDefinition Height="auto" />
                            </Grid.RowDefinitions>
                            <UniformGrid x:Name="TrayUniformGrid" />
                            <Button Click="SkipPickClick" Grid.Row="1" Width="200" Height="60"  FontSize="25" HorizontalAlignment="Center" Margin="0 30 0 5">Skip Pick</Button>
                            <Button Click="RetryPickClick" Grid.Row="2" Width="200" Height="60" FontSize="25" HorizontalAlignment="Center" Margin="0 5 0 30">Retry Pick</Button>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
