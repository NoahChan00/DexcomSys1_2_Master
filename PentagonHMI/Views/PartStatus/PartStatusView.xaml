<UserControl x:Class="PentagonHMI.Views.PartStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PentagonHMI.Views">
    <ItemsControl x:Name="ZoneListItemsControl" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <VirtualizingStackPanel VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <GroupBox Style="{StaticResource TopHeaderGroupBoxStyle}" Header="{Binding ZoneName}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" MinWidth="80" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource KeyTextBlockStyle}" Text="Part"
                                   Visibility="{Binding EnableManualAudit, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <ComboBox Grid.Row="0" Grid.Column="1" Style="{StaticResource ComboBoxStyle}" DisplayMemberPath="PartIndex"
                                  HorizontalContentAlignment="Center" ItemsSource="{Binding PartListView}"
                                  Visibility="{Binding EnableManualAudit, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <Button Grid.Row="0" Grid.Column="2" Style="{StaticResource ButtonStyle}" Content="Manual Audit" HorizontalAlignment="Left"
                                Visibility="{Binding EnableManualAudit, Converter={StaticResource BooleanToVisibilityConverter}}" />

                        <ItemsControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" ItemsSource="{Binding PartListView}"
                                      VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="1" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <Border Grid.Row="0" BorderBrush="Black" BorderThickness="1">
                                            <TextBlock Text="{Binding PartCount}" Background="{Binding PartStatus}" TextAlignment="Center" />
                                        </Border>

                                        <TextBlock Grid.Row="1" Text="{Binding PartIndex}" TextAlignment="Center">
                                            <TextBlock.ToolTip>
                                                <VirtualizingStackPanel VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
                                                    <TextBlock Style="{StaticResource CenterKeyTextBlockStyle}" Text="{Binding PartIndex}" />

                                                    <ItemsControl ItemsSource="{Binding PartSubStatusListView}"
                                                                  VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <VirtualizingStackPanel VirtualizingPanel.IsVirtualizing="True"
                                                                                        VirtualizingPanel.VirtualizationMode="Recycling" />
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <VirtualizingStackPanel Orientation="Horizontal" VirtualizingPanel.IsVirtualizing="True"
                                                                                        VirtualizingPanel.VirtualizationMode="Recycling">
                                                                    <TextBlock Style="{StaticResource KeyTextBlockStyle}"
                                                                               Text="{Binding SubStatusName, StringFormat={}{0} : }" />

                                                                    <TextBlock Style="{StaticResource ValueTextBlockStyle}" Text="{Binding SubStatus}" />
                                                                </VirtualizingStackPanel>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </VirtualizingStackPanel>
                                            </TextBlock.ToolTip>
                                        </TextBlock>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </GroupBox>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>