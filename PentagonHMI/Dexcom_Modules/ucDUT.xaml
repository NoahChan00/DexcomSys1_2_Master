<UserControl x:Class="PentagonHMI.ChildControls.ucDUT"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0"/>
            </Grid.ColumnDefinitions>

            <!--<Border CornerRadius=" 14,14,14,14" BorderBrush="{DynamicResource BlueLineBrush}" Margin="0,0,5,0" BorderThickness="2">
                <StackPanel>
                    <Label Content="Soc" Grid.ColumnSpan="2" Style="{DynamicResource styTopPenalBlue}" Height="30" FontSize="16" Padding="7 5 5 0" />
                    <Border Margin="5 2" BorderBrush="{DynamicResource BlueLineBrush}" BorderThickness="1" Height="50">
                        <TextBlock FontSize="15" Text="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Margin="5 2" BorderBrush="{DynamicResource BlueLineBrush}" BorderThickness="1" Height="50">
                        <TextBlock FontSize="15" Text="2" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Margin="5 2" BorderBrush="{DynamicResource BlueLineBrush}" BorderThickness="1" Height="50">
                        <TextBlock FontSize="15" Text="3" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Margin="5 2" BorderBrush="{DynamicResource BlueLineBrush}" BorderThickness="1" Height="50">
                        <TextBlock FontSize="15" Text="4" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Margin="5 2" BorderBrush="{DynamicResource BlueLineBrush}" BorderThickness="1" Height="50">
                        <TextBlock FontSize="15" Text="5" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Margin="5 2" BorderBrush="{DynamicResource BlueLineBrush}" BorderThickness="1" Height="50">
                        <TextBlock FontSize="15" Text="6" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Margin="5 2" BorderBrush="{DynamicResource BlueLineBrush}" BorderThickness="1" Height="50">
                        <TextBlock FontSize="15" Text="7" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Margin="5 2" BorderBrush="{DynamicResource BlueLineBrush}" BorderThickness="1" Height="50">
                        <TextBlock FontSize="15" Text="8" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Margin="5 2" BorderBrush="{DynamicResource BlueLineBrush}" BorderThickness="1" Height="50">
                        <TextBlock FontSize="15" Text="9" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Margin="5 2" BorderBrush="{DynamicResource BlueLineBrush}" BorderThickness="1" Height="50">
                        <TextBlock FontSize="15" Text="10" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Margin="5 2" BorderBrush="{DynamicResource BlueLineBrush}" BorderThickness="1" Height="50">
                        <TextBlock FontSize="15" Text="11" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                </StackPanel>
            </Border>-->

            <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="ItmC_Sockets" >
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="8" Rows="1" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius=" 14,14,14,14" MaxHeight="300" BorderBrush="{DynamicResource BlueLineBrush}" Margin="0,0,5,30" BorderThickness="2">
                                <StackPanel>
                                    <Label Content="{Binding Name}" Style="{DynamicResource styTopPenalBlue}" Height="30" FontSize="18" Padding="80,2,2,0" />
                                    <ItemsControl ItemsSource="{Binding Sockets}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Margin="5 2 5 0" BorderBrush="{DynamicResource BlueLineBrush}" BorderThickness="1">
                                                    <StackPanel Background="{Binding Background}" Height="50">
                                                        <TextBlock FontSize="15" Text="{Binding TotalPass}" HorizontalAlignment="Center"/>
                                                        <Separator/>
                                                        <TextBlock FontSize="15" Text="{Binding TotalFail}" HorizontalAlignment="Center"/>
                                                        <Separator/>
                                                        <TextBlock FontSize="15" Text="{Binding Yield}" HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!--<Border Grid.Column="2" CornerRadius=" 14,14,14,14" BorderBrush="{DynamicResource BlueLineBrush}" Margin="5,0,5,0" BorderThickness="2">
                <StackPanel>
                    <Label Content="Legends" Grid.ColumnSpan="2" Style="{DynamicResource styTopPenalBlue}" Height="30" FontSize="18" Padding="30,2,2,0" />
                    <StackPanel Width="240" Background="White" Name="Legends" Margin="5" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>-->
        </Grid>
    </ScrollViewer>
</UserControl>
