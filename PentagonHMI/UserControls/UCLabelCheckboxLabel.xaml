<UserControl x:Class="PentagonHMI.UserControls.UCLabelCheckboxLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Name="ucLabelCheckboxLabel" d:DesignWidth="230">
    <Grid>
        <Grid>
            <CheckBox Style="{DynamicResource styCheckBox}" HorizontalAlignment="Left"  Margin="20,0,0,0" Name="ucCheckBox" MinWidth="24" IsThreeState="True" IsChecked="True" VerticalAlignment="Center" />
            <Label Margin="0,5,0,5" Name="ucLabelTitle" Content="{Binding ElementName=ucLabelCheckboxLabel, Path=ucLabelTitleContent}" Height="33" HorizontalAlignment="Left" Width="15" FontWeight="Bold" VerticalAlignment="Center" />
            <Label Margin="40,8,0,5" Name="ucLabelContent" Height="30" Content="1000" VerticalAlignment="Center" />
        </Grid>
    </Grid>
</UserControl>