<UserControl x:Class="PentagonHMI.UserControls.UCAlarmTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PentagonHMI.UserControls"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="Lbl" TargetType="Label">
            <Setter Property="Background" Value="DimGray" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="Black" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="1" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="1" />
            <Setter Property="Height" Value="25" />
        </Style>
    </UserControl.Resources>
    <StackPanel>
        <Border BorderBrush="Black" BorderThickness="1 1 1 0">
            <StackPanel  Orientation="Horizontal">
                <Label Background="IndianRed" Content="Error Code" Width="102" Style="{StaticResource Lbl}" />
                <Label Content="Module" Width="102" Style="{StaticResource Lbl}" />
                <Label Content="DateTime" Width="227" Style="{StaticResource Lbl}" />
                <Label Content="Message" Width="682" Style="{StaticResource Lbl}" />
                <Label Content="Action" Width="682" Style="{StaticResource Lbl}" />
                <Label Width="19" Style="{StaticResource Lbl}" />
            </StackPanel>
        </Border>
        <Border BorderBrush="Black" BorderThickness="1 0 1 1" Height="200">
            <ScrollViewer>
                <ItemsControl ItemsSource="{Binding AlarmCollection}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:UCErrorMessage ErrorCode="{Binding ErrorCode}" DateTime="{Binding DateTime}"
                             Module="{Binding Module}" Message="{Binding Message}"  Action="{Binding Action}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </StackPanel>
</UserControl>