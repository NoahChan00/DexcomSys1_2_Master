<UserControl x:Class="PentagonHMI.UserControls.UCStationStatus"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="Status" TargetType="Rectangle">
            <Setter Property="Width" Value="166" />
            <Setter Property="Height" Value="130" />
            <Setter Property="Opacity" Value="0.6" />
            <Setter Property="Fill" Value="Gray" />
            <Style.Triggers>
                <EventTrigger RoutedEvent="Rectangle.Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.6" To="0.95" AutoReverse="True" Duration="0:0:1" RepeatBehavior="Forever" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LabelStatus" TargetType="Label">
            <Setter Property="Background" Value="Gray" />
            <Setter Property="Opacity" Value="0.8" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Height" Value="20" />
            <Setter Property="Padding" Value="10 1 10 1" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Content" Value="Loading" />
        </Style>

        <Style x:Key="DisLabel" TargetType="Label">
            <Setter Property="Background" Value="Black" />
            <Setter Property="Opacity" Value="0.9" />
            <Setter Property="Width" Value="75" />
            <Setter Property="Height" Value="120" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Content" Value="Disabled" />
            <Setter Property="Padding" Value="2" />
            <Setter Property="Visibility" Value="Collapsed" />
        </Style>
    </UserControl.Resources>
    <Canvas>
        <Rectangle Name="Rec_Status" Style="{StaticResource Status}" />
        <Label Name="Lbl_Status" Canvas.Top="132" Style="{StaticResource LabelStatus}" />
        <Label Name="Lbl_Zone1" Canvas.Left="6" Canvas.Top="5" Style="{StaticResource DisLabel}" />
        <Label Name="Lbl_Zone2" Canvas.Left="86" Canvas.Top="5" Style="{StaticResource DisLabel}" />
    </Canvas>
</UserControl>