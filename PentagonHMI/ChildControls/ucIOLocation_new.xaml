<UserControl x:Class="PentagonHMI.ChildControls.ucIOLocation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <Button x:Name="btnBack" Visibility="Collapsed" Click="Button_Click" Content="BACK" HorizontalAlignment="Right" FontSize="20" Padding="25 5" VerticalAlignment="Top" Margin="10"/>

            <Image Visibility="Collapsed" x:Name="Fullscreen" PreviewMouseLeftButtonDown="StackPanel_PreviewMouseLeftButtonDown" 
                        Height="{Binding SystemParameters.PrimaryScreenHeight}"  Width="{Binding SystemParameters.PrimaryScreenWidth}"
                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                        Stretch="Uniform"/>

            <Border x:Name="ioListBorder" CornerRadius="16" BorderThickness="2" BorderBrush="{DynamicResource GridLineBrush}" Margin="0">
                <Grid VerticalAlignment="Top">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Label Grid.Row="0" Content="IO Location List" Style="{DynamicResource styTopPenalBlue}"  FontSize="18" Padding="30,0,0,0"/>
                    <StackPanel x:Name="stackPanel" Panel.ZIndex="1" Grid.Row="1" Orientation="Horizontal">
                        <ItemsControl x:Name="itemControls">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel MaxWidth="1800"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel PreviewMouseLeftButtonDown="StackPanel_PreviewMouseLeftButtonDown" Tag="{Binding}" Margin="5" Orientation="Vertical">
                                        <Image Margin="5" Width="550" Height="450" Stretch="Uniform" Source="{Binding Img}"/>
                                        <TextBlock Margin="0" FontSize="20" FontWeight="Bold"  Text="{Binding Name}"  VerticalAlignment="Center" HorizontalAlignment="Center" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
