<UserControl x:Class="PentagonHMI.MachineInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PentagonHMI"
             TextElement.FontSize="14" TextElement.FontWeight="Regular">

    <GroupBox x:Name="MachineInformationGroupBox" Header="Information"
              Style="{StaticResource TopHeaderGroupBoxStyle}">
        <!--<ScrollViewer CanContentScroll="False" HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Disabled">-->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <ItemsControl Grid.Column="0" ItemsSource="{Binding MachineInformationToggleStatusListView}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Style="{StaticResource IndicatorCheckBoxStyle}" IsHitTestVisible="False"
                                      Content="{Binding MachineInformationToggleStatusName}"
                                      IsChecked="{Binding MachineInformationToggleStatus}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <ItemsControl Grid.Column="1" ItemsSource="{Binding MachineInformationTextListView}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="4"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" MinWidth="110"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0" Style="{StaticResource KeyTextBlockStyle}"
                                           Text="{Binding MachineInformationTextName}"/>
                                
                                <TextBlock Grid.Column="1" Style="{StaticResource ValueTextBlockStyle}"
                                           Text="{Binding MachineInformationTextValue, StringFormat={}: {0}}"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        <!--</ScrollViewer>-->
    </GroupBox>

</UserControl>